server:
  port: 8080

spring:
  application:
    name: fintech-gateway

  datasource:
    url: jdbc:postgresql://localhost:5432/fintech_gateway
    username: postgres
    password: root


  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true